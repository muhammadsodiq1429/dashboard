import { memo } from "react";

const ChildrenLiterature = () => {
  return (
    <div className="ChildrenLiterature">
      <h2>ChildrenLiterature</h2>
    </div>
  );
};

export default memo(ChildrenLiterature);
